// Задание 1
// Установка и настройка проекта
// 1. Создайте новый React-проект и установите в него Material UI
// npm install @mui/material @emotion/react @emotion/styled
// 2. Импорты
// Импортируйте необходимые компоненты: React, хук `useState` и компоненты Material UI, такие как `AppBar`, `Toolbar`, `Typography`, `TextField`, `Button`, `Container`, `Box`, и `Paper`.
// 3. Создание состояний
// Используйте хук `useState` для хранения имени, email и данных, которые были отправлены
// 4. Функция обработки отправки формы
// В `handleSubmit` предотвратите стандартное поведение формы, сохраняйте введенные данные и очищайте поля ввода.
// 5. Изучение документации
// Просмотрите документацию импортированных Material UI компонентов, подумайте, какие атрибуты вам могут понадобиться.
//     1. Разметка компонента
// Создайте верхнюю панель (AppBar) с заголовком (Typography):
// Используйте компонент `AppBar` для создания верхней панели.
// Внутри `AppBar` используйте `Toolbar` для размещения заголовка.
// Добавьте `Typography` с атрибутом `variant="h6"` для заголовка.
// Создайте контейнер (Container) для размещения содержимого:
// Внутри `Container` используйте `Box` для добавления отступов.
// Создайте карточку (Paper) для формы:
// Внутри `Box` добавьте `Paper` с атрибутом `elevation={3}` для создания тени.
// Внутри `Paper` добавьте `Typography` с атрибутом `variant="h5"` и `gutterBottom` для заголовка формы.
// Создайте форму для ввода данных:
// Добавьте два текстовых поля (TextField) для ввода имени и email.
// Используйте атрибут `label` для добавления метки к полю.
// Используйте атрибут `variant="outlined"` для создания поля с рамкой.
// Используйте атрибут `fullWidth` для растягивания поля на всю ширину контейнера.
// Используйте атрибут `type="email"` для поля email.
// Добавьте кнопку (Button) для отправки формы.
// Используйте атрибут `variant="contained"` для создания кнопки с фоном.
// Используйте атрибут `color="primary"` для установки основного цвета кнопки.
// Используйте атрибут `fullWidth` для растягивания кнопки на всю ширину контейнера.
// Используйте атрибут `disabled` для отключения кнопки, если поля ввода пустые.
//     2. Отображение данных
// Добавьте блок для отображения введенных данных после отправки формы:
// Используйте `Box` для добавления отступов.
// Внутри `Box` добавьте `Paper` с атрибутом `elevation={3}`.
// Внутри `Paper` добавьте `Typography` с атрибутами `variant="h6"` и `gutterBottom` для заголовка.
// Используйте `Typography` для отображения введенного имени и email.
//     3. Запуск приложения
// Сохраните изменения и запустите приложение.
// Проверьте, что всё выглядит и работает так, как должно
// Попробуйте поменять значения атрибутов Material UI компонентов, чтобы изменить внешний вид приложения.
// Попробуйте добавить новые компоненты Material UI, чтобы привнести дополнительный функционал в приложение.